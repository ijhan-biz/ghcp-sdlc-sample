name: "fieldops-codeql-config"

disable-default-queries: false

queries:
  - uses: security-and-quality

paths:
  - src
  - assets
  - scripts

paths-ignore:
  - .github/agents
  - .github/ref
  - docs
